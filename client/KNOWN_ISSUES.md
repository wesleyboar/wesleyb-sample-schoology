1. Must have unit tests beyond the default one.
2. Must have minimal build of React, not `create-react-app`.
3. Should have [ESLint]([eslint]) support/config.
4. Ideally, would support `postcss-preset-env` Stage 0.
